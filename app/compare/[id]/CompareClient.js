"use client";

import { useState } from "react";

export default function CompareClient({ initialData, params }) {
  const [product, setProduct] = useState(initialData);
  // 기존의 클라이언트 로직을 여기로 이동
  // ...
}
